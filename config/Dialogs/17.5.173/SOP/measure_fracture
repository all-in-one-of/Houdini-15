// Magic: cec959d7
// Automatically generated PSI dialog (PSI version 17.5.173)
// Sat Sep 28 18:40:49 2019


f_tmp = FLOAT_FIELD;	// dialog.top.ui

measureBBox.gad = ROW
{
    HSTRETCH

    { WIDTH(1.5) }
    { WIDTH(0.08) }

    PARM_BUTTON("measureBBox", "Bounding Box Size") toggle VALUE(measureBBox.val);

    { HSTRETCH }

}

measureArea.gad = ROW
{
    HSTRETCH

    { WIDTH(1.5) }
    { WIDTH(0.08) }

    PARM_BUTTON("measureArea", "Area") toggle VALUE(measureArea.val);

    { HSTRETCH }

}

measureVolume.gad = ROW
{
    HSTRETCH

    { WIDTH(1.5) }
    { WIDTH(0.08) }

    PARM_BUTTON("measureVolume", "Volume") toggle VALUE(measureVolume.val);

    { HSTRETCH }

}

sizeFilter.gad = 
{
    LAYOUT(horizontal) JUSTIFY(left, center) HSTRETCH

    PARM_LABEL_SIMPLE("Size Filter", "sizeFilter") WIDTH(1.5);
    { WIDTH(0.09) }

    sizeFilter.menu;

    { HSTRETCH }

}

sizeThreshold.gad = 
{
 LAYOUT(horizontal) HSTRETCH

 sizeThreshold.label = PARM_LABEL("Size Threshold", "sizeThreshold") WIDTH(1.5);
 PARM(float, 1, "sizeThreshold", none, none) RANGE(0,10)
	 LABELREF(sizeThreshold.label) HSTRETCH;

}

dialog_norm.gad = 
{
    LAYOUT(vertical) SPACING(0.02) HSTRETCH LOOK(plain) 
    measureBBox.gad; 
    measureArea.gad; 
    measureVolume.gad; 
    sizeFilter.gad; 
    sizeThreshold.gad; 
} 
// dialog.bot.ui

extrastuffstowbar_minimize.val := 1;

dialog = OPENNOTIFIER
{
    LAYOUT(vertical) SPACING(0.02) LOOK(bg) VALUE(open.val) HSTRETCH

    { HEIGHT(1p) LOOK(bg) HSTRETCH }

    dialog_norm.gad;
}
