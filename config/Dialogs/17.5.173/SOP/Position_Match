// Magic: 19c41c9e
// Automatically generated PSI dialog (PSI version 17.5.173)
// Sun Aug 18 22:21:01 2019


f_tmp = FLOAT_FIELD;	// dialog.top.ui
enable_x.row.join =		
{
  LAYOUT(horizontal) HSTRETCH JUSTIFY(left, center)

enable_x.gad = ROW
{
    

    PARM_BUTTON("enable_x", "x") toggle VALUE(enable_x.val);

    {  }

}

enable_y.gad = ROW
{
    

    PARM_BUTTON("enable_y", "y") toggle VALUE(enable_y.val);

    {  }

}

enable_z.gad = ROW
{
    

    PARM_BUTTON("enable_z", "z") toggle VALUE(enable_z.val);

    {  }

}

view_menu.gad = 
{
    LAYOUT(horizontal) JUSTIFY(left, center) 

    PARM_LABEL_SIMPLE("View", "view_menu") ;
    { WIDTH(0.09) }

    view_menu.menu;

    {  }

}
}	// Template join.bot


bound_frame.gad = 
{
 LAYOUT(horizontal) HSTRETCH

 bound_frame.label = PARM_LABEL("Bound Frame", "bound_frame") WIDTH(1.5);
 PARM(integer, 1, "bound_frame", none, none) RANGE(0,3000)
	 LABELREF(bound_frame.label) HSTRETCH;

}

lower_bound.gad = ROW
{
   HSTRETCH

   lower_bound.label = PARM_LABEL("Lower bound", "lower_bound") WIDTH(1.5);
   PARM(float, 3, "lower_bound", none, none, "X,Y,Z")
	RANGE(-1,1)  LABELREF(lower_bound.label)
	HSTRETCH;

}

upper_bound.gad = ROW
{
   HSTRETCH

   upper_bound.label = PARM_LABEL("Upper Bound", "upper_bound") WIDTH(1.5);
   PARM(float, 3, "upper_bound", none, none, "X,Y,Z")
	RANGE(-1,1)  LABELREF(upper_bound.label)
	HSTRETCH;

}

bound_x_dist_lookup.si_rampedit = FLOAT_RAMP VALUE(bound_x_dist_lookup.ramp_val)
			LAYOUT(horizontal)  HSTRETCH;

bound_x_dist_lookup.multiramp = 
{
    LAYOUT(vertical) STRETCH

    ROW
    {
	LOOK(plain) JUSTIFY(left,center) SPACING(0.1) VMARGIN(0.02) HSTRETCH

	COL
	{
	    JUSTIFY(right,center) WIDTH(1.5)
	    LABEL "Point No.";
	}

	bound_x_dist_lookup.pointno.gad
	    = INT_SLIDER_FIELD VALUE(bound_x_dist_lookup.pointno.val) HSTRETCH;
    }
}

bound_x_dist_lookup.buttons.gad = COL
{
    VSTRETCH

    ICON_BUTTON_STRIP
    {
	LAYOUT(horizontal) INITSIZE(20p, 20p) 

	ACTION_ICONBUTTON "BUTTONS_delete"
		HELP("Remove Point") VALUE(bound_x_dist_lookup.delpoint.val);
	ACTION_ICONBUTTON "BUTTONS_add"
		HELP("Add Point") VALUE(bound_x_dist_lookup.addpoint.val);
    }

    { VSTRETCH }

    ROW
    {
	    bound_x_dist_lookup.toggleparms.gad
	      = ACTION_ICONBUTTON "BUTTONS_show_controls"
		HELP("Hide Controls")
		VALUE(bound_x_dist_lookup.toggle.val)
		SENDSKEY("h.pane.parms.toggle_ramp_parms")
		LOOKFLAT UI_FLAT_ICON_BTN_SIZE_TINY;

	    bound_x_dist_lookup.togglerampsize.gad
	      = ACTION_ICONBUTTON "BUTTONS_restore_mini"
		HELP("Maximize ramp")
		VALUE(bound_x_dist_lookup.togglerampsize.val)
		SENDSKEY("h.pane.parms.toggle_ramp_size")
		LOOKFLAT UI_FLAT_ICON_BTN_SIZE_TINY;
    }
}

bound_x_dist_lookup.parm =
{
    LAYOUT(vertical) HSTRETCH JUSTIFY(left, center)

    bound_x_dist_lookup.ramp.gad = ROW
    {
	MARGIN(0.02) SPACING(0.02) JUSTIFY(left, top) HSTRETCH

	bound_x_dist_lookup.buttons.gad VSTRETCH;

	bound_x_dist_lookup.si_rampedit HSTRETCH;
    }
}

bound_x_dist_lookup.gad =
{
    LAYOUT(vertical) MARGIN(2p) HSTRETCH

    {
	LAYOUT(horizontal) HSTRETCH

	PARM_LABEL_SIMPLE("X Dist Lookup", "bound_x_dist_lookup");

	{ HSTRETCH }

    }

    {
	LAYOUT(vertical) MARGIN(2p) SPACING(0.02) LOOK(darkline) HSTRETCH

	bound_x_dist_lookup.parm;

	SWITCHER tight
	{
	    LAYOUT(overlap) VALUE(bound_x_dist_lookup.hideparms.val) HSTRETCH
	    bound_x_dist_lookup.multiramp;
	    {}
	}
    }
}

bound_y_dist_lookup.si_rampedit = FLOAT_RAMP VALUE(bound_y_dist_lookup.ramp_val)
			LAYOUT(horizontal)  HSTRETCH;

bound_y_dist_lookup.multiramp = 
{
    LAYOUT(vertical) STRETCH

    ROW
    {
	LOOK(plain) JUSTIFY(left,center) SPACING(0.1) VMARGIN(0.02) HSTRETCH

	COL
	{
	    JUSTIFY(right,center) WIDTH(1.5)
	    LABEL "Point No.";
	}

	bound_y_dist_lookup.pointno.gad
	    = INT_SLIDER_FIELD VALUE(bound_y_dist_lookup.pointno.val) HSTRETCH;
    }
}

bound_y_dist_lookup.buttons.gad = COL
{
    VSTRETCH

    ICON_BUTTON_STRIP
    {
	LAYOUT(horizontal) INITSIZE(20p, 20p) 

	ACTION_ICONBUTTON "BUTTONS_delete"
		HELP("Remove Point") VALUE(bound_y_dist_lookup.delpoint.val);
	ACTION_ICONBUTTON "BUTTONS_add"
		HELP("Add Point") VALUE(bound_y_dist_lookup.addpoint.val);
    }

    { VSTRETCH }

    ROW
    {
	    bound_y_dist_lookup.toggleparms.gad
	      = ACTION_ICONBUTTON "BUTTONS_show_controls"
		HELP("Hide Controls")
		VALUE(bound_y_dist_lookup.toggle.val)
		SENDSKEY("h.pane.parms.toggle_ramp_parms")
		LOOKFLAT UI_FLAT_ICON_BTN_SIZE_TINY;

	    bound_y_dist_lookup.togglerampsize.gad
	      = ACTION_ICONBUTTON "BUTTONS_restore_mini"
		HELP("Maximize ramp")
		VALUE(bound_y_dist_lookup.togglerampsize.val)
		SENDSKEY("h.pane.parms.toggle_ramp_size")
		LOOKFLAT UI_FLAT_ICON_BTN_SIZE_TINY;
    }
}

bound_y_dist_lookup.parm =
{
    LAYOUT(vertical) HSTRETCH JUSTIFY(left, center)

    bound_y_dist_lookup.ramp.gad = ROW
    {
	MARGIN(0.02) SPACING(0.02) JUSTIFY(left, top) HSTRETCH

	bound_y_dist_lookup.buttons.gad VSTRETCH;

	bound_y_dist_lookup.si_rampedit HSTRETCH;
    }
}

bound_y_dist_lookup.gad =
{
    LAYOUT(vertical) MARGIN(2p) HSTRETCH

    {
	LAYOUT(horizontal) HSTRETCH

	PARM_LABEL_SIMPLE("Y Dist Lookup", "bound_y_dist_lookup");

	{ HSTRETCH }

    }

    {
	LAYOUT(vertical) MARGIN(2p) SPACING(0.02) LOOK(darkline) HSTRETCH

	bound_y_dist_lookup.parm;

	SWITCHER tight
	{
	    LAYOUT(overlap) VALUE(bound_y_dist_lookup.hideparms.val) HSTRETCH
	    bound_y_dist_lookup.multiramp;
	    {}
	}
    }
}

bound_z_dist_lookup.si_rampedit = FLOAT_RAMP VALUE(bound_z_dist_lookup.ramp_val)
			LAYOUT(horizontal)  HSTRETCH;

bound_z_dist_lookup.multiramp = 
{
    LAYOUT(vertical) STRETCH

    ROW
    {
	LOOK(plain) JUSTIFY(left,center) SPACING(0.1) VMARGIN(0.02) HSTRETCH

	COL
	{
	    JUSTIFY(right,center) WIDTH(1.5)
	    LABEL "Point No.";
	}

	bound_z_dist_lookup.pointno.gad
	    = INT_SLIDER_FIELD VALUE(bound_z_dist_lookup.pointno.val) HSTRETCH;
    }
}

bound_z_dist_lookup.buttons.gad = COL
{
    VSTRETCH

    ICON_BUTTON_STRIP
    {
	LAYOUT(horizontal) INITSIZE(20p, 20p) 

	ACTION_ICONBUTTON "BUTTONS_delete"
		HELP("Remove Point") VALUE(bound_z_dist_lookup.delpoint.val);
	ACTION_ICONBUTTON "BUTTONS_add"
		HELP("Add Point") VALUE(bound_z_dist_lookup.addpoint.val);
    }

    { VSTRETCH }

    ROW
    {
	    bound_z_dist_lookup.toggleparms.gad
	      = ACTION_ICONBUTTON "BUTTONS_show_controls"
		HELP("Hide Controls")
		VALUE(bound_z_dist_lookup.toggle.val)
		SENDSKEY("h.pane.parms.toggle_ramp_parms")
		LOOKFLAT UI_FLAT_ICON_BTN_SIZE_TINY;

	    bound_z_dist_lookup.togglerampsize.gad
	      = ACTION_ICONBUTTON "BUTTONS_restore_mini"
		HELP("Maximize ramp")
		VALUE(bound_z_dist_lookup.togglerampsize.val)
		SENDSKEY("h.pane.parms.toggle_ramp_size")
		LOOKFLAT UI_FLAT_ICON_BTN_SIZE_TINY;
    }
}

bound_z_dist_lookup.parm =
{
    LAYOUT(vertical) HSTRETCH JUSTIFY(left, center)

    bound_z_dist_lookup.ramp.gad = ROW
    {
	MARGIN(0.02) SPACING(0.02) JUSTIFY(left, top) HSTRETCH

	bound_z_dist_lookup.buttons.gad VSTRETCH;

	bound_z_dist_lookup.si_rampedit HSTRETCH;
    }
}

bound_z_dist_lookup.gad =
{
    LAYOUT(vertical) MARGIN(2p) HSTRETCH

    {
	LAYOUT(horizontal) HSTRETCH

	PARM_LABEL_SIMPLE("Z Dist Lookup", "bound_z_dist_lookup");

	{ HSTRETCH }

    }

    {
	LAYOUT(vertical) MARGIN(2p) SPACING(0.02) LOOK(darkline) HSTRETCH

	bound_z_dist_lookup.parm;

	SWITCHER tight
	{
	    LAYOUT(overlap) VALUE(bound_z_dist_lookup.hideparms.val) HSTRETCH
	    bound_z_dist_lookup.multiramp;
	    {}
	}
    }
}

volume_mult.gad = 
{
 LAYOUT(horizontal) HSTRETCH

 volume_mult.label = PARM_LABEL("Volume Mult", "volume_mult") WIDTH(1.5);
 PARM(float, 1, "volume_mult", none, none) RANGE(0,10)
	 LABELREF(volume_mult.label) HSTRETCH;

}

folder2.gad = 
GROUP simple "Lookup"
{ 
    VALUE(folder2.gad.val) LAYOUT(vertical) HSTRETCH 
	    bound_x_dist_lookup.gad;		
	    bound_y_dist_lookup.gad;		
	    bound_z_dist_lookup.gad;		
} 

folder1.gad = 
GROUP simple "Bound"
{ 
    VALUE(folder1.gad.val) LAYOUT(vertical) HSTRETCH 
	    bound_frame.gad;		
	    lower_bound.gad;		
	    upper_bound.gad;		
} 

folder0.gad = 
GROUP simple "Enable"
{ 
    VALUE(folder0.gad.val) LAYOUT(vertical) HSTRETCH 
	    enable_x.row.join;		
} 

dialog_norm.gad = 
{
    LAYOUT(vertical) SPACING(0.02) HSTRETCH LOOK(plain) 
    folder0.gad; 
    folder1.gad; 
    folder2.gad; 
    volume_mult.gad; 
} 
// dialog.bot.ui

extrastuffstowbar_minimize.val := 1;

dialog = OPENNOTIFIER
{
    LAYOUT(vertical) SPACING(0.02) LOOK(bg) VALUE(open.val) HSTRETCH

    { HEIGHT(1p) LOOK(bg) HSTRETCH }

    dialog_norm.gad;
}
